SELECT worker.first_name, worker.last_name, car.model, GROUP_CONCAT(child.name SEPARATOR ',') AS Kids FROM `worker`
LEFT JOIN `car` ON car.user_id = worker.id LEFT JOIN `child` ON child.user_id = worker.id GROUP BY worker.id